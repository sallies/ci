version: 2

jobs:
  build:
    docker:
      - image: gaozhi/mysql-client-alpine
      - image: circleci/mysql:5.6
    steps:
      - run:
          name: Greeting
          command: sleep 10
      - run:
          name: Greeting
          command: |
            mysql -u root -h 127.0.0.1  -e 'show databases;'
